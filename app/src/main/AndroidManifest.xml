<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yourdomain.project50">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:appComponentFactory="whateverString"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:appComponentFactory">
        <activity
            android:name=".Activitys.ScaleActivity"
            android:theme="@style/SpalishTheme"></activity>
        <activity
            android:name=".Activitys.SpalishActivity"
            android:theme="@style/SpalishTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activitys.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activitys.EachPlanExcersizesActivity"
            android:label="@string/title_activity_all_excersizes"
            android:theme="@style/SpalishTheme" />
        <activity android:name=".Activitys.ExcersizeListActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".Activitys.ExcersizeActivity"
            android:theme="@style/SpalishTheme" />
        <activity android:theme="@style/SpalishTheme" android:name=".Activitys.CongragulationActivity"
            ></activity>
        <service android:name=".TTSHelper"/>
    </application>

</manifest>